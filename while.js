let val = 0
while (val <= 11) {
    console.log(`The value is ${val}`);
    val = val + 2
    
}

let myArr = ["irfan", "Ali", "Ahmad"]
let arr = 0
while (arr < myArr.length) {
    console.log(`The Name is ${myArr[arr]}`)
    arr = arr + 1           // we can use arr++
}